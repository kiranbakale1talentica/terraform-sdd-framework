name: 'Terraform Destroy'

on:
  workflow_dispatch:
    inputs:
      service:
        description: 'Service to destroy (e.g. docs-portal)'
        required: true
        default: 'docs-portal'
        type: string
      confirm_destroy:
        description: 'Type "DESTROY" to confirm'
        required: true
        type: string

permissions:
  id-token: write # Required for AWS OIDC
  contents: read

env:
  TF_VERSION: "1.10.3"
  AWS_REGION: "us-west-1"
  OIDC_ROLE_ARN: "arn:aws:iam::682563173581:role/terraform-sdd-actions-role"

jobs:
  destroy:
    name: Destroy ${{ github.event.inputs.service }}/dev
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./terraform/services/${{ github.event.inputs.service }}/dev

    steps:
      - name: Verify Confirmation Input
        run: |
          if [ "${{ github.event.inputs.confirm_destroy }}" != "DESTROY" ]; then
            echo "Error: Confirmation input did not match 'DESTROY'."
            exit 1
          fi

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS Credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.OIDC_ROLE_ARN }}
          role-session-name: GitHubActionsSession
          aws-region: ${{ env.AWS_REGION }}
          audience: sts.amazonaws.com

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Init
        run: terraform init

      - name: Terraform Destroy
        run: terraform destroy -auto-approve -input=false
